---
name: danjjak-frontend
description: Implement assigned Danjjak Vue screens using linked specifications and the existing frontend organization.
---

# danjjak-frontend

- Read the assigned issue, linked feature rules, and [shared UX](../../../docs/specs/requirements/shared-ux.md).
- Use Vue 3, Vite, Pinia, Vue Router and `<script setup>` SFCs. Use the planned `views`, `api`, `components/common`, `composables`, `features`, `stores`, and `router` organization; see [frontend plan](../../../frontend/README.md).
- Use `PascalCase.vue` and `camelCase`; keep API calls outside presentation components and use the configured backend origin during implementation.
- Current user decisions and revised specs take priority over stale Figma/code. No reference image or Figma file has been supplied; do not invent approved assets.
- Preserve four tabs, readable Korean, large touch areas, manual alternatives, actual persisted results, and the complete demo flow. Add shared components only for real reuse.
- Implement only the assigned outcome. Run the relevant frontend build and linked scenarios once tooling exists.
